[project]
name = "so-101-studio-policy"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
policy = [
  "lerobot[dataset] @ git+https://github.com/huggingface/lerobot.git",
]

[tool.uv]
package = false
